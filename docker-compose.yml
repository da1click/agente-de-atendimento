services:
  agente:
    image: SEU_USUARIO_DOCKERHUB/agente-de-atendimento:latest
    container_name: agente-de-atendimento
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - stack.env
    volumes:
      - ./clientes:/app/clientes
